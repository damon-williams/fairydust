# Environment configuration
ENVIRONMENT=staging

# Service authentication (for registering MCP app)
FAIRYDUST_SERVICE_TOKEN=your-service-token-here

# Optional: Port for health check endpoint
PORT=8080